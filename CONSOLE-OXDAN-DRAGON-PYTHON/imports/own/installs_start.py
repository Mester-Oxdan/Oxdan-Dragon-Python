import os

def installs_start(name):

            if name == "python-3.10.6":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\python.exe")
                
            if name == "nmap-7.94-setup":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\nmap.exe")
                
            if name == "git":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\git.exe")
                
            if name == "virtualbox":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\virtualbox.exe")
                
            if name == "protonvpn":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\protonvpn.exe")
                
            if name == "wireshark":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\wireshark.exe")
                
            if name == "miniconda":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\miniconda3.exe")
                
            if name == "tor":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\tor.exe")
                
            if name == "torrent":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\torrent.exe")
                
            if name == "vscode":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\vs_code.exe")
                
            if name == "vs":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\vs.exe")
                
            if name == "steam":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\steam.exe")
                
            if name == "ea":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\ea.exe")
                
            if name == "7_zip":
                os.system("start " + os.environ["OXDAN-DRAGON-PYTHON"] + "\\imports\\own\\imports\\installs\\7_zip.exe")