import imports.own.will_go_to_start

def Elses():

    if imports.own.will_go_to_start.x.lower() == "exit" or imports.own.will_go_to_start.x.lower() == "esc" or imports.own.will_go_to_start.x.lower() == "quit": # exit (+)

            exit(0)